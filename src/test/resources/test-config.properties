# Local test configuration
# Update these values to match your local database
db.url=jdbc:mysql://127.0.0.1:3306/demo
db.user=root
db.password=
browser=chrome-headless
# Toggle Redis tests on (set to true if Redis is available locally or in CI)
tests.redis.enabled=true
